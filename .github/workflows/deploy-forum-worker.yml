name: Deploy forum worker

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-forum-worker
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          version: 10
          run_install: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: workers/forum/pnpm-lock.yaml
      - name: Deploy
        working-directory: workers/forum
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          pnpm install --frozen-lockfile
          pnpm test
          pnpm typecheck
          npx wrangler d1 execute reasonix-forum --remote --file=migrate-performance-indexes.sql
          npx wrangler deploy
